# Copy to .env and fill in. NEVER commit real secrets.
DATABASE_URL=postgresql://postgres:bidintel@localhost:5432/postgres
BEDROCK_MODEL_ID=mock-claude
MAX_RAG_RETRIES=2
# AWS (production only)
AWS_REGION=us-east-1
# COGNITO_POOL_ID=us-east-1_XXXXXXX
# SECRETS_NAME=bidintel/prod
